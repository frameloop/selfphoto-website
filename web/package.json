{
  "name": "selfphoto-website",
  "version": "1.0.0",
  "description": "Sitio web de SelfPhoto Studio",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "echo 'No build step required for static site'",
    "vercel-build": "echo 'No build step required for static site'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "website",
    "photography",
    "static"
  ],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "live-server": "^1.2.2",
    "postcss-cli": "^11.0.1",
    "sass": "^1.89.0"
  },
  "dependencies": {
    "puppeteer": "^24.8.2"
  }
}